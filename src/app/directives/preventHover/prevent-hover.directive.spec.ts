import { PreventHoverDirective } from './prevent-hover.directive';

describe('PreventHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventHoverDirective();
    expect(directive).toBeTruthy();
  });
});
